# スマホだけで将棋アプリを使う手順書

このアプリは**スマホのホーム画面に置いて、圏外でも動かせます**。
一度だけ「置き場所（URL）」を用意すれば、あとはPCを持ち歩く必要はありません。

---

## まず結論：どれを選ぶ？

| | 方法A：クラウドに置く | 方法B：とりあえず1人用 | 方法C：家のPCで動かす |
|---|---|---|---|
| 準備の手間 | 20〜30分（1回だけ） | 5分（1回だけ） | 済んでいる |
| 費用 | 無料 | 無料 | 無料 |
| 1人でCPと対局 | ◎ どこでも・圏外でも | ◎ どこでも・圏外でも | △ 家のWi-Fiだけ |
| 棋譜解析・段位測定 | ◎ | ◎ | △ 家のWi-Fiだけ |
| 1台を回して対人戦 | ◎ | ◎ | △ |
| **スマホ2台で対戦** | ◎ どこでも | ✕ 使えない | △ 家のWi-Fiだけ |
| PCを持ち歩く | 不要 | 不要 | 不要（家に置いておく） |

**2台対戦も外でやりたいなら方法A。** とりあえず自分ひとりで指したいだけなら方法B。

> 方法A・Bとも、**最初の1回だけMacを使います**（ファイルを置くため）。
> それ以降はスマホだけで完結します。

---

# 方法A：クラウドに置く（全部使える）

`server.js` ごとクラウドに置くので、1人用も2台対戦も、どこにいても使えます。

## A-1. GitHub にファイルを置く（Macで1回）

1. スマホでもPCでもよいので [github.com](https://github.com) でアカウントを作る（無料）
2. 右上の **＋** → **New repository**
3. Repository name に `shogi` と入れる → **Public** のまま → **Create repository**
4. 次の画面の **uploading an existing file** をクリック
5. Finder で **「将棋」フォルダを開き**、
   **中身を全部選択**（`index.html` `server.js` `package.json` `css` `js` `icons` `tools` `test` …）して、
   ブラウザの点線の枠にドラッグ＆ドロップ

   > ⚠️ フォルダごとドラッグしないでください。
   > `index.html` が**いちばん上の階層**にある必要があります。
   > うまくドラッグできないときは Chrome で試してください（Safariより確実です）。

6. 下の **Commit changes** を押す

## A-2. Render で公開する（Macで1回）

1. [render.com](https://render.com) を開き **Get Started**（GitHubアカウントでログイン）
2. **New +** → **Web Service**
3. さきほどの `shogi` リポジトリを選ぶ（初回は Connect account の許可が必要）
4. 設定を次のようにする

   | 項目 | 入れる値 |
   |---|---|
   | Name | `shogi`（何でもよい） |
   | Region | Singapore |
   | Branch | main |
   | Runtime | Node |
   | Build Command | （空欄のまま） |
   | Start Command | `node server.js` |
   | Instance Type | **Free** |

5. **Create Web Service** を押す
6. 2〜5分待つと、上のほうに **`https://shogi-xxxx.onrender.com`** のようなURLが出る
   → これがあなたのアプリのアドレスです。**メモしておく**

## A-3. スマホのホーム画面に入れる

**iPhone（Safariで開くこと）**

1. Safari で A-2 のURLを開く
2. 下の **共有ボタン**（□に↑）をタップ
3. **「ホーム画面に追加」** → **追加**
4. ホーム画面に将棋の駒のアイコンができる

**Android（Chromeで開くこと）**

1. Chrome でURLを開く
2. 右上の **︙** → **アプリをインストール**（または「ホーム画面に追加」）

**確認：** アプリを開いて、画面上部のバッジが
**「オフラインOK」（緑）** になっていれば保存完了です。
この状態なら**機内モード・圏外でも起動して対局できます**。

> 初回はエンジンと定跡（約120KB）を保存するため、開いたまま10秒ほど待ってください。

## A-4. スマホ2台で対戦する

1. **2台とも**同じURL（ホーム画面のアイコンでOK）を開く
2. 片方で「**2台で対戦**」タブ → 手番を選んで **部屋を作る** → 4桁の番号が出る
3. もう片方で「**2台で対戦**」タブ → その番号を入れて **参加**
4. あとは自分の手番のときだけ盤を触れます。盤は各自の側が手前になります

> **無料プランの注意**：15分ほど誰も使わないとサーバーが眠ります。
> その場合、最初のアクセスだけ表示に30〜60秒かかります（2回目からは速い）。
> 対局中に眠ることはありません。

---

# 方法B：1人用だけ、いちばん簡単（5分）

2台対戦は使えませんが、**対CP戦・棋譜解析・段位測定・1台での対人戦**は全部使えます。

1. Mac で [app.netlify.com/drop](https://app.netlify.com/drop) を開く
2. Finder の **「将棋」フォルダをそのままドラッグ＆ドロップ**
3. `https://〇〇〇.netlify.app` というURLが出る
4. **無料アカウントを作って保存**（作らないと数時間で消えます）
5. スマホでそのURLを開き、**A-3と同じ手順でホーム画面に追加**

「2台で対戦」タブは開けますが、「対戦サーバーにつながっていません」と出ます。
あとから方法Aのサーバーを用意したら、そのタブの **「対戦サーバー」** 欄に
`https://shogi-xxxx.onrender.com` を入れて **保存** すれば、2台対戦も使えるようになります。

---

# 方法C：家のPCで動かす（今のまま）

1. Mac で `start.command` をダブルクリック
2. ターミナルに出る `http://192.168.x.x:8777/` をスマホで開く
3. 同じWi-Fiにいる2台で「2台で対戦」ができる

この方法だけは**オフライン保存が効きません**（`https` ではないため）。
家の中で、Macを起動している間だけ使えます。

---

## ホーム画面に入れたあとの使い方

- **起動**：ホーム画面の駒アイコンをタップ。ブラウザのバーが出ない全画面で開きます
- **圏外でも動く**：対CP戦・棋譜解析・段位測定・1台での対人戦はすべてオフラインで動きます
- **棋譜の保存先**：その端末の中（localStorage）です。端末間では共有されません
  - 別の端末に渡したいときは「棋譜・解析」→ **KIFで書き出し** → 出てきた文字をコピーしてLINE等で送り、
    受け取った側で **KIF/SFENを読み込む** に貼り付け
- **アプリを更新したとき**：アプリを一度閉じて開き直すと新しくなります（2回開くと確実）

---

## うまくいかないときは

| 症状 | 対処 |
|---|---|
| バッジが「オフライン：不可」 | `https://` で開いているか確認。`http://192.168…` ではオフライン保存できません |
| 圏外だと真っ白 | まだ保存が終わっていません。電波のあるところで一度開き、10秒ほど待つ |
| 「エンジン：簡易モード」と出る | ファイルを直接開いています。方法A・BのURLから開いてください |
| 2台対戦がつながらない | 「対戦サーバー」欄にRenderのURLを入れて保存。2台とも同じサーバーを見る必要があります |
| 相手の手が来ない | 電波を確認。画面を閉じても、開き直せば続きから指せます |
| 最初のアクセスが遅い | Render無料プランがスリープから復帰中（30〜60秒） |
| 棋譜が消えた | ブラウザのデータを消すと消えます。残したい棋譜はKIFで書き出しを |
| 盤が小さい／大きい | 端末の画面サイズに自動で合わせています。横向きにすると盤が大きくなります |

---

## セキュリティについて

- URLを知っている人は誰でも開けます。個人情報は一切入力しません（名前は表示用のみ）
- 2台対戦の部屋番号は4桁です。念のため、対局が終わったら「対戦をやめる」を押してください
- 部屋は最後の操作から6時間で自動的に消えます
